- name: Install latest guest agent
  hosts: swarm
  become: true
  
  tasks:
  - name: Install latest qemu-guest-agent
    apt:
      name: qemu-guest-agent
      state: latest
  
  - name: Enable and start qemu-guest-agent
    service:
      name: qemu-guest-agent
      enabled: true
      state: started