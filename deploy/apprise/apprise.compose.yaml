services:
  apprise:
    image: caronc/apprise:v1.5.4
    container_name: apprise
    user: "1000:1000"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    networks:
      - apprise_network
    environment:
      TZ: Europe/Vienna
      APPRISE_STATEFUL_MODE: disabled
      APPRISE_WORKER_COUNT: 1
      APPRISE_API_ONLY: yes
    volumes:
      - /opt/swarm-data/apprise/tmp:/tmp
    deploy:
      mode: global

networks:
  apprise_network:
    external: true