services:
  whoami:
    image: traefik/whoami
    networks:
      - proxy_network
    deploy:
      replicas: 3
      labels:
        caddy_1: whoami.$INTERNAL_DOMAIN
        caddy_1.reverse_proxy: "{{upstreams 80}}"
        caddy_2: https://whoami.$EXTERNAL_DOMAIN
        caddy_2.reverse_proxy: "{{upstreams 80}}"

networks:
  proxy_network:
    external: true